# gh-coding-agent

A small React demo website built to showcase the GitHub Copilot Coding Agent.

## Pages

The site has exactly three pages, served by `react-router-dom`:

- **Home** (`/`) — landing page with quick links to the other pages.
- **About** (`/about`) — what the project is and how it’s built.
- **Robotics** (`/robotics`) — a short tour of robotics topics.

## Tech stack

- [React 18](https://react.dev/)
- [Vite](https://vitejs.dev/) for the dev server and production build
- [react-router-dom](https://reactrouter.com/) for client-side routing

## Getting started

```bash
npm install
npm run dev      # start the dev server at http://localhost:5173
npm run build    # build for production into ./dist
npm run preview  # preview the production build
```

## Screenshots

Screenshots of the three pages (captured at 1280×900) are attached to the
pull request that introduced this site.
